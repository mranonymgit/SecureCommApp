// lib/core/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF125973); // Tu azul principal
  static const Color accent = Color(0xFF0765BD);   // Color secundario
  static const Color background = Colors.white;
  static const Color textBody = Color(0xA1FFFFFF); // El blanco transparente que usabas
  static const Color alert = Colors.redAccent;
}